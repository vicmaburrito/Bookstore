import {
  addBook,
  PostToApi,
  getBooks,
  removeBook,
  DeleteBookApi,
  listBooks,
} from './books';

const allActions = {
  addBook,
  PostToApi,
  getBooks,
  removeBook,
  DeleteBookApi,
  listBooks,
};

export default allActions;
